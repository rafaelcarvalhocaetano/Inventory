

import 'package:flutter/widgets.dart';

kCardText (colors) => TextStyle(
                              fontFamily: 'OpenSans',
                              fontSize: 14.0,
                              color: colors,
                            );
const kCardTitle = TextStyle(
                              fontSize: 17.0,
                              fontFamily: 'OpenSans',
                              fontWeight: FontWeight.bold,
                            );